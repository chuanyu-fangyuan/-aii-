# AI 情报站 · 设计思路

## 设计动机

每天泡在 Hacker News、TechCrunch、arXiv 上看 AI 资讯，有两个痛点反复出现：

一是**碎片化**。同一条新闻的不同侧面散落在不同来源——Hacker News 有社区讨论，TechCrunch 有深度报道，arXiv 有原始论文——但它们各自独立，我需要手动把它们关联起来。

二是**信息密度**。看完一堆标题后常常记不住：这周大家都在聊什么话题？哪些方向在同时被多家媒体关注？单点阅读无法回答这些问题。

所以我想做一个东西，把多个独立信息源聚合到一处，并提供一个**关联视角**来观察资讯之间的结构——而不仅仅是按时间排列。

## 核心灵感：从 Obsidian 到新闻图谱

我日常用 Obsidian 搭建个人知识库，每天写、每天整理笔记。Obsidian 的核心体验是两件事：**双向链接**（`[[ ]]`）和**关系图谱**。图谱让我从"我写了哪些笔记"这个一维问题，变成"我的知识网络长什么样"这个二维问题。

这个体验直接启发了 AI 情报站的"关系图谱"设计——

| Obsidian | AI 情报站 |
|----------|----------|
| 节点 = 我的笔记 | 节点 = 一条资讯 |
| 节点 = 一个关键词/标签 | 节点 = 一个 AI 关键词 |
| 边 = 笔记之间的 `[[ ]]` 链接 | 边 = 资讯中**共享的关键词** |
| 我的笔记越多，图谱越有意义 | 资讯积累越多，话题簇越清晰 |

没有 LLM，没有 NLP，图谱的构建逻辑和 Obsidian 一样朴素：**两篇笔记都提到同一个词，它们之间就有一条隐式关联**。换成资讯也成立——两条新闻都提到"Jensen Huang"或"Anthropic"，它们就天然属于同一个话题簇。

## 架构决策：为什么选静态站 + 定时采集

Obsidian 对我的另一个影响是**数据自主**——笔记是纯 Markdown，存在本地，不依赖任何服务。这让我倾向于同样的思路：

**后端采集（Python + SQLite）→ 导出静态 data.json → 原生前端 + D3.js 渲染**

选这个架构是权衡了三件事：

1. **零运行成本**。GitHub Actions + GitHub Pages 免费，不用租服务器，不用维护数据库服务。
2. **可复现**。任何机器拉下来跑 `pip install feedparser && python fetch.py` 就能复现全链路，评审成本极低。
3. **数据所有权**。SQLite 数据库和导出的 JSON 都在仓库里，数据采集逻辑完全透明——信息源是谁、抓取了什么、怎么去重的，没有任何黑箱。

不做 Node.js 后端、不做 Next.js、不做云数据库，是刻意的减法：这个项目不需要动态页面，不需要用户系统，不需要实时推送。把这些都加上只会增加复杂度而不增加价值。

## 关键技术取舍

### 关键词提取：实体词典 + 标题高频词，不用 LLM

README 里也提过，这是出于两个考虑：

- **零成本**。LLM 调用虽然单次便宜，但每天 200 条新闻跑起来会累积，而且我不想引入任何付费依赖。
- **可解释**。实体词典在 `fetch.py` 顶部一眼就能看到，60 多个常见 AI 实体可以直接增减。相比之下 LLM 提取的关键词"不知道它为什么会选这个词"，调试起来像黑盒。

实践下来发现效果其实够用——"噪声词"问题通过**只用标题统计频率**、**设 ≥2 次阈值**、**停用词表扩充**三轮迭代后基本消失了。

### 时间口径：UTC 存储 + 展示层转换

这也是从 Obsidian 学到的：metadata 存原始值，展示层自己决定怎么显示。项目里发布时间和采集时间分离存储，内部统一 UTC ISO8601，前端用 `Intl.DateTimeFormat` 转 Asia/Shanghai。

这么做的好处：以后要改成显示英文、换时区、或者加"相对时间（X 小时前）"都不用动数据层。坏处是采集脚本和前端都得处理时区转换——一开始写 HN 解析的时候忘了 `tzinfo` 导致所有时间被当成 UTC 00:00，踩过一次坑后就记住了。

### 容错策略：区分"无新增"和"抓取失败"

在采集脚本里我把单源结果分成三种状态：`ok`（有新增）、`no_new`（源返回了但没新东西）、`failed`（真的出错了）。区分它们的原因是**反馈语义不一样**——`no_new` 可能是正常（arXiv 周末本来就不更新），`failed` 才需要用户去检查。

这比简单的 "成功 / 失败" 二值更有信息量，也是后来排查 HN 数据陈旧问题时的关键依据——当时日志一直显示 `ok` 但数据三个星期没变化，后来才发现 HN 查询串被 API 错误解析、只返回了 24 条旧帖子。从那之后我加了**数据新鲜度校验**的自检步骤。

### 日期的语义边界

"今天 = 北京时间当日 00:00 起"，"近 7 天 = 当前时刻向前 7×24h"——这两个定义看起来没毛病，但实际跑起来会出现：欧美媒体在中国时间的"今天"里还没发稿（时差），用户以为是抓取失败。

所以前端加了引导：当「今天 + 某来源」筛选为空时，提示"可能是时区差异，不是抓取失败"，并提供一键切换到近 7 天的快捷操作。**不把产品逻辑藏起来，而是把它解释给用户**。

## 项目结构与分工

```
fetch.py              ← 采集 + 去重 + 导出（单文件 468 行，含实体词典和停用词表）
data/news.db          ← SQLite 存储，保留全部历史
web/data.json         ← fetch.py 生成，供前端消费（最近 30 天窗口）
web/index.html        ← 双视图（资讯流 / 关系图谱）+ 抽屉详情
web/app.js            ← 主逻辑：状态、过滤、渲染、抽屉
web/graph.js          ← D3.js 力导向图：拖拽 / 缩放 / 高亮
web/styles.css        ← 暗色主题，CSS 变量，移动端适配
.github/workflows/    ← GitHub Actions cron + 手动触发
```

一个 Python 文件搞定后端，三个 JS 文件搞定前端，没有任何框架、没有任何构建步骤——这是我能接受的最简复杂度。

## 踩过的坑（也是判断依据）

| 坑 | 暴露了什么 | 修了之后学到什么 |
|----|----------|----------------|
| HN 查询串带引号导致 API 误解析，连续三周只返回旧帖 | 采集成功 ≠ 数据正确 | 每次采集后必须校验数据新鲜度 |
| arXiv 摘要模板前缀 `arXiv:xxx...Abstract:` 混进关键词 | 高频词统计信噪比过低 | 高频词只从标题统计，不用摘要 |
| 页脚和卡片布局重叠（`height: 100%` 锁死 body）| flex 布局的 `min-height` 陷阱 | `min-height: 100vh` 才是正确姿势 |
| 关键词图谱出现 `Abstract`、`across` 等噪声 | 停用词表不够 | 补充停用词 + 实体词典优先匹配 |

每个坑都反过来倒逼架构决策——比如"数据新鲜度校验"就是因为 HN 那次事故加进去的。**问题比设计文档更能驱动架构演化**。
